<%= simple_form_for @case_file, url: update_assignment_admin_case_path(@case_file) do |f| %>

  <%= f.input :user_id, as: :select, collection: @users.collect { |u| [ u.name, u.id ] } %>
  <%= f.submit 'Save', class: 'btn btn-primary btn-block' %>

<% end %>
